name: Deploy Frontend to VPS

on:
  push:
    branches:
      - integrazione

jobs:
  deploy:
    name: Deploy FE
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Copy source and rebuild frontend
        run: |
          cp -r . ${{ secrets.VPS_PROJECT_PATH }}/sito-presentazione-banda-fe-new
          cd ${{ secrets.VPS_PROJECT_PATH }}
          docker compose up --build -d frontend
          echo "✅ FE Deploy completato"
